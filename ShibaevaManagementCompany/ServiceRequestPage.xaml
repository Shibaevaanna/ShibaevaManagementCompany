<Page x:Class="ShibaevaManagementCompany.ServiceRequestsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Управление заявками">

    <Grid Background="White" Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Page Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Управление заявками" 
                       FontSize="24" FontWeight="Bold" 
                       FontFamily="Segoe UI"/>
            <TextBlock Text="Создание, редактирование и отслеживание заявок на ремонт и обслуживание" 
                       FontSize="14" FontFamily="Segoe UI" 
                       Foreground="#666666" Margin="0,5,0,0"/>
        </StackPanel>

        <!-- Toolbar -->
        <Border Grid.Row="1" Background="#F4E8D3" Padding="15" CornerRadius="5" Margin="0,0,0,20">
            <StackPanel Orientation="Horizontal">
                <Button Name="AddButton" Content="+ Добавить заявку" 
                        Width="150" Height="35" Click="AddButton_Click"/>
                <Button Name="EditButton" Content="✏️ Редактировать" 
                        Width="150" Height="35" Margin="10,0" Click="EditButton_Click"/>
                <Button Name="DeleteButton" Content="🗑️ Удалить" 
                        Width="150" Height="35" Click="DeleteButton_Click" Background="#E74C3C"/>

                <Separator Width="20" Visibility="Hidden"/>

                <ComboBox Name="FilterComboBox" Width="150" Height="35" Margin="10,0"
                          SelectedIndex="0">
                    <ComboBoxItem Content="Все заявки"/>
                    <ComboBoxItem Content="Новые"/>
                    <ComboBoxItem Content="В работе"/>
                    <ComboBoxItem Content="Выполненные"/>
                </ComboBox>

                <TextBox Name="SearchTextBox" Width="200" Height="35" Margin="10,0" 
                         Padding="10,0" VerticalContentAlignment="Center"
                         TextChanged="SearchTextBox_TextChanged">
                    <TextBox.Style>
                        <Style TargetType="TextBox">
                            <Style.Triggers>
                                <Trigger Property="IsFocused" Value="False">
                                    <Setter Property="Foreground" Value="#999999"/>
                                    <Setter Property="Text" Value="Поиск по адресу или ФИО..."/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </TextBox.Style>
                </TextBox>

                <Button Name="RefreshButton" Content="🔄 Обновить" 
                        Width="120" Height="35" Click="RefreshButton_Click"/>
            </StackPanel>
        </Border>

        <!-- Data Grid -->
        <Border Grid.Row="2" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="5">
            <DataGrid Name="RequestsDataGrid" 
                      AutoGenerateColumns="False"
                      SelectionChanged="RequestsDataGrid_SelectionChanged">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding RequestId}" Width="60"/>
                    <DataGridTextColumn Header="Адрес" Binding="{Binding Address}" Width="250"/>
                    <DataGridTextColumn Header="ФИО заявителя" Binding="{Binding ApplicantName}" Width="180"/>
                    <DataGridTextColumn Header="Телефон" Binding="{Binding Phone}" Width="120"/>
                    <DataGridTextColumn Header="Описание" Binding="{Binding Description}" Width="300"/>
                    <DataGridTextColumn Header="Исполнитель" Binding="{Binding Assignee}" Width="150"/>
                    <DataGridTextColumn Header="Статус" Binding="{Binding Status}" Width="100">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="HorizontalAlignment" Value="Center"/>
                                <Setter Property="Padding" Value="5"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Status}" Value="Новая">
                                        <Setter Property="Background" Value="#3498DB"/>
                                        <Setter Property="Foreground" Value="White"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding Status}" Value="В работе">
                                        <Setter Property="Background" Value="#F39C12"/>
                                        <Setter Property="Foreground" Value="White"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding Status}" Value="Выполнена">
                                        <Setter Property="Background" Value="#2ECC71"/>
                                        <Setter Property="Foreground" Value="White"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="Дата создания" Binding="{Binding CreatedDate}" Width="120"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</Page>